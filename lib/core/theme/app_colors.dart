import 'package:flutter/material.dart';

abstract final class AppColors {
  // ── Backgrounds ─────────────────────────────────────────────
  static const Color background = Color(0xFF070B14);

  static const Color surface = Color(0xFF0C121E);

  static const Color card = Color(0xFF131A2A);

  static const Color cardElevated = Color(0xFF1A2438);

  // ── Inputs ──────────────────────────────────────────────────
  static const Color inputFill = Color(0xFF131A2A);

  static const Color inputFillFocused = Color(0xFF1A2438);

  // ── Borders & Dividers ──────────────────────────────────────
  static const Color border = Color(0xFF2A3654);

  static const Color borderSubtle = Color(0xFF1A2438);

  static const Color borderStrong = Color(0xFF3B4B72);

  static const Color divider = Color(0xFF1D263B);

  // ── Text ────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFFFFFFFF);

  static const Color textSecondary = Color(0xFF8B9BB4);

  static const Color textTertiary = Color(0xFF637492);

  static const Color textDisabled = Color(0xFF475877);

  // ── Icons ───────────────────────────────────────────────────
  static const Color icon = Color(0xFFFFFFFF);

  static const Color iconSecondary = Color(0xFF8B9BB4);

  static const Color iconDisabled = Color(0xFF475877);

  // ── Buttons ─────────────────────────────────────────────────
  static const Color buttonPrimary = Color(0xFFFFFFFF);

  static const Color onButtonPrimary = Color(0xFF070B14);

  static const Color buttonSecondary = Color(0xFF1A2438);

  static const Color onButtonSecondary = Color(0xFFFFFFFF);

  static const Color buttonDisabled = Color(0xFF131A2A);

  static const Color onButtonDisabled = Color(0xFF475877);

  // ── Semantic ────────────────────────────────────────────────
  static const Color error = Color(0xFFFF453A);

  static const Color errorBackground = Color(0xFF1C0A09);

  static const Color success = Color(0xFF30D158);

  static const Color successBackground = Color(0xFF071A0E);

  static const Color warning = Color(0xFFFFD60A);

  static const Color warningBackground = Color(0xFF1A1700);

  // ── Social ──────────────────────────────────────────────────
  static const Color like = Color(0xFFFF375F);

  static const Color verified = Color(0xFF0A84FF);

  // ── Utility ─────────────────────────────────────────────────
  static const Color shimmerBase = Color(0xFF131A2A);

  static const Color shimmerHighlight = Color(0xFF2A3654);

  static const Color overlay = Color(0x9903050B);
}
